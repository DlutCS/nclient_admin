import 'angular'


'use strict';

module.exports = function (app) {

    app.controller('userLeftCtrl', ['$rootScope', '$scope', '$window', '$document', '$timeout', '$state', '$q',
        function ($rootScope, $scope, $window, $document, $timeout, $state, $q) {
            console.log('User Left 控制器');
            
           
            
        }
    ]);
};
