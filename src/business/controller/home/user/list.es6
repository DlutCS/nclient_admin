import 'angular'


'use strict';

export default function (app) {

    app.controller('userListCtrl', ['$rootScope', '$scope', '$window', '$document', '$timeout', '$state', '$q',
        function ($rootScope, $scope, $window, $document, $timeout, $state, $q) {
            console.log('User List 控制器');
            
           
            
        }
    ]);
};
