import 'angular'


'use strict';

module.exports = function (app) {

    app.controller('controlLeftCtrl', ['$rootScope', '$scope', '$window', '$document', '$timeout', '$state', '$q',
        function ($rootScope, $scope, $window, $document, $timeout, $state, $q) {
            console.log('Control Left 控制器');
            
           
            
        }
    ]);
};
