import 'angular'


'use strict';

module.exports = function (app) {

    app.controller('categoryListCtrl', ['$rootScope', '$scope', '$window', '$document', '$timeout', '$state', '$q',
        function ($rootScope, $scope, $window, $document, $timeout, $state, $q) {
            console.log('Category List 控制器');
            
           
            
        }
    ]);
};
