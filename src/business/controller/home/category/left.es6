import 'angular'


'use strict';

module.exports = function (app) {

    app.controller('categoryLeftCtrl', ['$rootScope', '$scope', '$window', '$document', '$timeout', '$state', '$q',
        function ($rootScope, $scope, $window, $document, $timeout, $state, $q) {
            console.log('Category Left 控制器');
            
           
            
        }
    ]);
};
