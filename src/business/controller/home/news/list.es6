import 'angular'


'use strict';

export default function (app) {

    app.controller('newsListCtrl', ['$rootScope', '$scope', '$window', '$document', '$timeout', '$state', '$q',
        function ($rootScope, $scope, $window, $document, $timeout, $state, $q) {
            console.log('news List 控制器');
            
           
            
        }
    ]);
};
