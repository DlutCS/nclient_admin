import 'angular'


'use strict';

module.exports = function (app) {

    app.controller('newsLeftCtrl', ['$rootScope', '$scope', '$window', '$document', '$timeout', '$state', '$q',
        function ($rootScope, $scope, $window, $document, $timeout, $state, $q) {
            console.log('News Left 控制器');
            
           
            
        }
    ]);
};
