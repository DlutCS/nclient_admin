import 'angular'


'use strict';

module.exports = function (app) {

    app.controller('loginCtrl', ['$rootScope', '$scope', '$window', '$document', '$timeout', '$state', '$q',
        function ($rootScope, $scope, $window, $document, $timeout, $state, $q) {
            console.log('login controller');
            
           
            
        }
    ]);
};
